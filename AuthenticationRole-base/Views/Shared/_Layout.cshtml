<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"] - بلو جرين للتنمية الزراعية</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.rtl.min.css" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />
    <link rel="stylesheet"  href="~/css/layout.css"/>


    @RenderSection("Styles", required: false)
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar">
        <div class="navbar-container">
            <a href="/" class="logo">
                <img src="~/images/blue-green-logo.png" alt="Blue Green Logo" class="logo-img">
            </a>

            <!-- قائمة الروابط الأفقية للشاشات الكبيرة -->
            <ul class="desktop-nav">
                <button class="button-outline" style="visibility:hidden">تسجيل الخروج</button>
                <li><a href="/" class="active" style="padding-top:10px">الرئيسية</a></li>
                <li><a asp-action="Products" asp-controller="Product">المنتجات</a></li>
                <li><a asp-action="Articles" asp-controller="Article">المقالات</a></li>
                <li><a asp-action="LAB" asp-controller="Home">المعمل</a></li>
                <li><a asp-action="About" asp-controller="Home">من نحن</a></li>
                <li><a asp-action="Questions" asp-controller="Home">الأسئلة الشائعة</a></li>
                <li><a asp-action="Contact" asp-controller="Home">تواصل معنا</a></li>
                <partial name="_LoginPartial" />
              
            </ul>

            <button class="nav-toggle" id="sidebarToggle">
                <i class="bi bi-list"></i>
            </button>

            
        </div>
    </nav>

    <!-- Sidebar Menu -->
    <div class="sidebar" id="sidebar">
        <div class="sidebar-header">
            <img src="~/images/blue-green-logo.png" alt="Blue Green Logo" class="sidebar-logo">
            <button class="sidebar-close" id="sidebarClose">
                <i class="bi bi-x-lg"></i>
            </button>
        </div>

        <ul class="sidebar-nav">
            <li><a href="/" class="active">الرئيسية</a></li>
            <li><a asp-action="Products" asp-controller="Product">المنتجات</a></li>
            <li><a asp-action="Articles" asp-controller="Article">المقالات</a></li>
            <li><a asp-action="LAB" asp-controller="Home">المعمل</a></li>
            <li><a asp-action="About" asp-controller="Home">من نحن</a></li>
            <li><a asp-action="Questions" asp-controller="Home">الأسئلة الشائعة</a></li>
            <li><a asp-action="Contact" asp-controller="Home">تواصل معنا</a></li>
            <hr />
            <li>
                <a asp-action="Contact" asp-controller="Home">
                    <button class="btn btn-light" style="">طلب عرض أسعار</button>
                </a>
            </li>
            <partial name="_LoginPartial"/>
        </ul>
    </div>

    <!-- Sidebar Backdrop -->
    <div class="sidebar-backdrop" id="sidebarBackdrop"></div>

    <!-- Main Content -->
    <main>
        @RenderBody()
    </main>

    <!-- Footer -->
    <footer class="footer">
        <div class="footer-grid">
            <div class="footer-column">
                <h3>تواصل معنا</h3>
                <p><i class="bi bi-geo-alt ms-2"></i> أبو غالب، الظهير الصحراوي طريق القاهرة - الإسكندرية</p>
                <p><i class="bi bi-telephone ms-2"></i> هاتف: 01004514461</p>
                <p><i class="bi bi-envelope ms-2"></i> البريد الإلكتروني: info@bluegreen.com</p>
                <div class="social-links">
                    <a href="https://www.facebook.com/profile.php?id=100069043569459"><i class="bi bi-facebook"></i></a>
                    @*
                    <a href="#"><i class="bi bi-twitter"></i></a>
                    <a href="#"><i class="bi bi-instagram"></i></a>
                    <a href="#"><i class="bi bi-youtube"></i></a>
                    *@ 
               </div>
            </div>

            <div class="footer-column">
                <h3>روابط مهمة</h3>
                <ul>
                    <li><a href="/" class="active">الرئيسية</a></li>
                    <li><a asp-action="Products" asp-controller="Product">المنتجات</a></li>
                    <li><a asp-action="Articles" asp-controller="Article">المقالات</a></li>
                    <li><a asp-action="LAB" asp-controller="Home">المعمل</a></li>
                    <li><a asp-action="About" asp-controller="Home">من نحن</a></li>
                    <li><a asp-action="Questions" asp-controller="Home">الأسئلة الشائعة</a></li>
                    <li><a asp-action="Contact" asp-controller="Home">تواصل معنا</a></li>
                </ul>
            </div>
            <div class="footer-column">
                <h3>عن بلو جرين</h3>
                <p>شركة رائدة في مجال التنمية الزراعية المستدامة، نسعى لتطوير القطاع الزراعي من خلال تقديم حلول مبتكرة وخدمات متميزة.</p>
                <p>هدفنا المساهمة في تحقيق الأمن الغذائي وتعزيز التنمية المستدامة.</p>
            </div>
        </div>
        <div class="copyright">
            <a href="/" class="logo">
                <img src="~/images/blue-green-logo.png" alt="Blue Green Logo" class="logo-img">
            </a>
            <p>جميع الحقوق محفوظة &copy; @DateTime.Now.Year بلو جرين للتنمية الزراعية</p>
        </div>
    </footer>

    <!-- WhatsApp Button -->
    <a href="https://wa.me/+201004514461" class="whatsapp-button">
        <i class="bi bi-whatsapp"></i>
    </a>

    <script src="https://cdn.jsdelivr.net/npm/jquery@3.6.0/dist/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // عناصر القائمة الجانبية
            const sidebarToggle = document.getElementById('sidebarToggle');
            const sidebarClose = document.getElementById('sidebarClose');
            const sidebar = document.getElementById('sidebar');
            const sidebarBackdrop = document.getElementById('sidebarBackdrop');

            // فتح القائمة الجانبية
            sidebarToggle.addEventListener('click', function() {
                sidebar.classList.add('active');
                sidebarBackdrop.classList.add('active');
                // منع التمرير في الصفحة الرئيسية عند فتح القائمة
                document.body.style.overflow = 'hidden';
            });

            // إغلاق القائمة الجانبية
            function closeSidebar() {
                sidebar.classList.remove('active');
                sidebarBackdrop.classList.remove('active');
                // السماح بالتمرير مرة أخرى
                document.body.style.overflow = '';
            }

            // إغلاق بالضغط على زر الإغلاق
            sidebarClose.addEventListener('click', closeSidebar);

            // إغلاق بالضغط على الخلفية
            sidebarBackdrop.addEventListener('click', closeSidebar);

            // إغلاق عند الضغط على أي رابط في القائمة
            const sidebarLinks = sidebar.querySelectorAll('a');
            sidebarLinks.forEach(link => {
                link.addEventListener('click', function() {
                    closeSidebar();
                });
            });

            // إضافة الصنف النشط للرابط الحالي (للقائمة الجانبية)
            const currentLocation = window.location.pathname;
            const menuItems = document.querySelectorAll('.sidebar-nav a');
            menuItems.forEach(item => {
                const href = item.getAttribute('href');
                if (href === currentLocation) {
                    menuItems.forEach(link => link.classList.remove('active'));
                    item.classList.add('active');
                }
            });

            // إضافة الصنف النشط للرابط الحالي (للقائمة الأفقية)
            const desktopMenuItems = document.querySelectorAll('.desktop-nav a');
            desktopMenuItems.forEach(item => {
                const href = item.getAttribute('href');
                if (href === currentLocation) {
                    desktopMenuItems.forEach(link => link.classList.remove('active'));
                    item.classList.add('active');
                }
            });
        });
    </script>
    @RenderSection("Scripts", required: false)
</body>
</html>