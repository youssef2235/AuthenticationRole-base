@model ProductDto
@{
    Layout = null;
}

<pre>


</pre>


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"]</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }

        :root {
            --primary: #4361ee;
            --secondary: #3f37c9;
            --success: #4cc9f0;
            --danger: #f72585;
            --warning: #f8961e;
            --info: #4895ef;
            --dark: #212529;
            --light: #f8f9fa;
            --sidebar-width: 250px;
        }

        body {
            background-color: #f5f7fb;
            display: flex;
            min-height: 100vh;
        }

        .sidebar {
            width: var(--sidebar-width);
            background-color: var(--dark);
            color: white;
            position: fixed;
            height: 100vh;
            transition: all 0.3s;
        }

        .sidebar-header {
            padding: 20px;
            text-align: center;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

            .sidebar-header h3 {
                margin-bottom: 10px;
            }

        .menu-items {
            padding: 20px 0;
        }

        .menu-item {
            padding: 15px 20px;
            cursor: pointer;
            transition: all 0.3s;
            display: flex;
            align-items: center;
        }

            .menu-item:hover {
                background-color: rgba(255, 255, 255, 0.1);
            }

            .menu-item.active {
                background-color: var(--primary);
            }

            .menu-item i {
                margin-right: 10px;
            }

        .main-content {
            flex: 1;
            margin-left: var(--sidebar-width);
            padding: 20px;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            padding-bottom: 20px;
            border-bottom: 1px solid #eee;
        }

        .user-info {
            display: flex;
            align-items: center;
        }

            .user-info img {
                width: 40px;
                height: 40px;
                border-radius: 50%;
                margin-right: 10px;
            }

        .dashboard-cards {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .card {
            background-color: white;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            transition: transform 0.3s;
        }

            .card:hover {
                transform: translateY(-5px);
            }

        .card-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 10px;
            border-bottom: 1px solid #eee;
        }

        .card-icon {
            width: 50px;
            height: 50px;
            border-radius: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 24px;
        }

        .card-body {
            margin-bottom: 20px;
        }

        .card-value {
            font-size: 28px;
            font-weight: bold;
            margin-bottom: 5px;
        }

        .card-description {
            color: #6c757d;
            font-size: 14px;
        }

        .card-footer {
            display: flex;
            justify-content: space-between;
            font-size: 14px;
        }

        .trend {
            display: flex;
            align-items: center;
        }

            .trend.up {
                color: #28a745;
            }

            .trend.down {
                color: #dc3545;
            }

        .button {
            padding: 10px 15px;
            background-color: var(--primary);
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.3s;
            text-decoration: none;
            display: inline-block;
        }

            .button:hover {
                background-color: var(--secondary);
            }

        .recent-activity {
            background-color: white;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin-bottom: 30px;
        }

        .activity-item {
            display: flex;
            align-items: center;
            padding: 15px 0;
            border-bottom: 1px solid #eee;
        }

            .activity-item:last-child {
                border-bottom: none;
            }

        .activity-icon {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            margin-right: 15px;
        }

        .activity-details {
            flex: 1;
        }

            .activity-details h4 {
                margin-bottom: 5px;
            }

        .activity-time {
            color: #6c757d;
            font-size: 14px;
        }

    </style>
</head>
<body>
    <div class="sidebar">
        <div class="sidebar-header">
            <h3>Admin Dashboard</h3>
            <p>Welcome back, @(User.Identity?.Name ?? "Admin")</p>
        </div>
        <div class="menu-items">
            <div class="menu-item active">
                <i class="fas fa-home"></i>
                <span>Dashboard</span>
            </div>
            <div class="menu-item">
                <i class="fas fa-box"></i>
                <span>Products</span>
            </div>
            <div class="menu-item">
                <i class="fas fa-chart-line"></i>
                <span>Analytics</span>
            </div>
            <div class="menu-item">
                <i class="fas fa-users"></i>
                <span>Customers</span>
            </div>
            <div class="menu-item">
                <i class="fas fa-cog"></i>
                <span>Settings</span>
            </div>
            <div class="menu-item">
                <i class="fas fa-sign-out-alt"></i>
                <span>Logout</span>
            </div>
        </div>
    </div>

    <div class="main-content">
        <h2 class="text-center mb-5">List Of Products</h2>

        <div class="row mb-5">
            @if (User.IsInRole("admin"))
            {
                <div class="col">
                    <a class="btn btn-primary" asp-controller="Product" asp-action="Create">New Product</a>
                </div>
            }
            <pre>

    </pre>
        </div>

        <pre>


</pre>

        <div class="row">
            <div class="col-md-8 mx-auto rounded border p-3">
                <h2 class="text-center mb-5">تعديل بيانات منتج</h2>

                <form method="post" enctype="multipart/form-data" asp-action="Edit" asp-controller="Product">

                    <div class="row mb-3">
                        <div class="col-sm-8">
                            <input type="hidden" class="form-control-plaintext" readonly value="@ViewData["ProductId"]" />
                        </div>
                    </div>

                    <div class="row mb-3">
                        <label class="col-sm-4 col-form-label">الاسم</label>
                        <div class="col-sm-8">
                            <input asp-for="Name" class="form-control" />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="row mb-3">
                        <label class="col-sm-4 col-form-label">العلامة التجارية</label>
                        <div class="col-sm-8">
                            <input asp-for="Brand" class="form-control" />
                            <span asp-validation-for="Brand" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="row mb-3">
                        <label class="col-sm-4 col-form-label">الفئة</label>
                        <div class="col-sm-8">
                            <select asp-for="Category" class="form-control">
                                <option value="نباتات">نباتات</option>
                                <option value="أشجار مثمرة"> أشجار مثمرة</option>
                                <option value="امراض زراعية">امراض زراعية</option>
                                <option value="سماد عضوي">سماد عضوي</option>
                                <option value="ادويه حشرية">ادويه حشرية</option>
                            </select>
                        </div>
                    </div>

                    <div class="row mb-3">
                        <label class="col-sm-4 col-form-label">السعر</label>
                        <div class="col-sm-8">
                            <input asp-for="Price" class="form-control" />
                            <span asp-validation-for="Price" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="row mb-3">
                        <label class="col-sm-4 col-form-label">الكمية</label>
                        <div class="col-sm-8">
                            <input asp-for="Pages" class="form-control" />
                            <span asp-validation-for="Pages" class="text-danger"></span>
                        </div>
                    </div>

                    <div class="row mb-3">
                        <label class="col-sm-4 col-form-label">وصف المنتج</label>
                        <div class="col-sm-8">
                            <input asp-for="Description" class="form-control" />
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>
                    </div>


                    <div class="row mb-3">
                        <div class="col-sm-8">
                            <img src="~/Products/@ViewData["ImageFileName"]" width="100" />
                        </div>
                    </div>

                    <div class="row mb-3">
                        <label class="col-sm-4 col-form-label">صورة المنتج</label>
                        <div class="col-sm-8">
                            <input asp-for="ImageFile" type="file" class="form-control" />
                            <span asp-validation-for="ImageFile" class="text-danger"></span>
                        </div>
                    </div>


                    <div class="row mb-3">
                        <label class="col-sm-4 col-form-label">وقت الاضافة في المخزون</label>
                        <div class="col-sm-8">
                            <input class="form-control-plaintext" readonly value="@ViewData["DateTime"]" />
                        </div>
                    </div>

                    <div class="row">
                        <div class="offset-sm-4 col-sm-4 d-grid">
                            <button type="submit" class="btn btn-primary">حفظ التعديل</button>
                        </div>

                        <div class="col-sm-4 d-grid">
                            <a asp-controller="Product" asp-action="Index" class="btn btn-secondary">الرجوع</a>
                        </div>
                    </div>
                </form>
            </div>
        </div>

<pre>


</pre>

</div>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script>
        $(document).ready(function() {
            // Menu toggle functionality
            $('.menu-item').on('click', function() {
                // Remove active class from all items
                $('.menu-item').removeClass('active');
                // Add active class to clicked item
                $(this).addClass('active');

                // If this is a real application, we would navigate to the corresponding page
                // var targetPage = $(this).data('target');
                // window.location.href = targetPage;
            });

            // Initialize dashboard data
            function updateDashboardCards() {
                // In a real app, you would use AJAX to fetch updated data from the server
                $.ajax({
                    url: '@Url.Action("GetDashboardStats", "Admin")',
                    type: 'GET',
                    success: function(data) {
                        // Update the dashboard with fresh data
                        console.log('Dashboard data updated');
                        // Would update DOM elements with the returned data
                    },
                    error: function(error) {
                        console.error('Error updating dashboard:', error);
                    }
                });
            }

            // Simulating periodic updates - in a real app this might be handled with SignalR
            setInterval(updateDashboardCards, 60000); // Update every minute
        });
    </script>
</body>
</html>
</pre>













